import React from "react";
import Image from "./Image";

describe("Image component", () => {
  it("renders the image with correct attributes", () => {});

  it("toggles favourite status when button is clicked", () => {});

  it("displays alt text on hover", () => {});

  it("displays missing image when there is an error loading the image", () => {});
});
