import Header from "./Header";

describe("Header Component", () => {
  it("should render the favicon", () => {});

  it("should render the app name", () => {});

  // as required by Giphy API usage policy
  it("should render the Giphy logo", () => {});
});
