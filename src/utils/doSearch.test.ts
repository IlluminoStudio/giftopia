import { doSearch } from "./doSearch";

describe("doSearch function", () => {
  it("sets showingTrending to true when searchTerm is undefined", async () => {});

  it("sets showingTrending to false when searchTerm is set", async () => {});

  it("calls fetchImages with searchTerm", async () => {});

  it("sets images with fetchImages results", async () => {});

  it("error handling when fetchImages throws", async () => {});
});
