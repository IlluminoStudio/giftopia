import { fetchImages } from "./fetchImages";

describe("fetchImages function", () => {
  it("performs trending fetch if no search term", async () => {});

  it("performs search if search term is provided", async () => {});

  it("handles empty response data", async () => {});

  it("handles API call errors", async () => {});
});
